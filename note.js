// ARRAYS

// Array = multiple values in a single variable

// Array properties -> (data about the array) .length is a property as an array is an object and .length will display that array' length property

// Array methods -> Predfined functions that are associated with the array and perform operations on it. Such as .map() which returns a new array after performing an operation on it -> such as arr = [1, 2, 3] arr.map(* 2) = newArr = [2, 4, 6]

